[project]
name = "extract-finetuning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.10.0",
    "datasets>=4.0.0",
    "gpustat>=1.1.1",
    "monai==1.5.2",
    "numpy>=2.4.5",
    "pillow>=11.3.0",
    "torch>=2.13.0",
    "transformers>=5.14.0",
    "wandb>=0.21.1",
]

[tool.uv]
override-dependencies = [
    "tokenizers>=0.23.1",
]
